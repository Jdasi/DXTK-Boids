#pragma once

// Namespace for storing mathematical function helpers.
namespace JMath
{
    float clampf(float _curr, float _min, float _max); // Normal clamp.
    float iclampf(float _curr, float _min, float _max); // Inverse clamp.
}
